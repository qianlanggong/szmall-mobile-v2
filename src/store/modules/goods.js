// state
const state = {
  name: "我是goods模块",
};
// mutations
const mutations = {};
// getters
const getters = {};
// actions
const actions = {
  // 获取首页商品的列表
  // async getProducts(context){}
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
